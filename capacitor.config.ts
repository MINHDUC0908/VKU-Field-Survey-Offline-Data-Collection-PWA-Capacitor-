import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'vn.edu.vku.facilityinspector',
  appName: 'VKU Facility Inspector',
  webDir: 'dist'
};

export default config;
